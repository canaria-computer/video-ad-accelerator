import { Container } from "@chakra-ui/react"
import React from "react"

import App from "~app"

function IndexPopup() {
  return (
    <>
      <Container minWidth={400} minHeight={300} m={20}>
        <App />
      </Container>
    </>
  )
}

export default IndexPopup
